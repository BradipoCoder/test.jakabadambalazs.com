/**
 * HOME
 */
(function ($, Drupal) {
    Drupal.behaviors.jabHome = {
        attach: function (context, settings) {
            var self = this;
            /* do something */
        },
        build: function (self) {
            /* do something */
        },
    };
})(jQuery, Drupal);
